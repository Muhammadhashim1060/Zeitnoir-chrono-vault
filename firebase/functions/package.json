{
  "name": "depth-ei-functions",
  "version": "0.1.0",
  "engines": { "node": "18" },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc -p .",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "npm run build && firebase deploy --only functions",
    "lint": "eslint . --ext .ts"
  },
  "dependencies": {
    "openai": "^4.57.0",
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^5.0.1",
    "cross-fetch": "^4.0.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.6.2",
    "ts-node": "^10.9.2",
    "eslint": "^9.13.0",
    "@types/node": "^20.12.12"
  }
}